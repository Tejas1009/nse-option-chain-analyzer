export class DerivativeModel{
    constructor(public ce: number,public pe: number, public diff: number, public pcr: number, public date: string, 
        public time: string, public price: number){
    }
}